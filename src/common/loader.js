
import Loader from "react-js-loader";

export const LoaderFile = () => {
return (
        <div className={"row"}>
            <div className={"item"}>
                <Loader type="hourglass" bgColor={"#2065d1"} size={80} />
            </div>
        </div>
)
}